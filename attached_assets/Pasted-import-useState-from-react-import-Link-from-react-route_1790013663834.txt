import { useState } from "react";
import { Link } from "react-router-dom";

export default function Navbar() {
  const [open, setOpen] = useState(false);

  return (
    <nav
      style={{
        position: "sticky",
        top: 0,
        zIndex: 999,
        background: "#fff",
        borderBottom: "1px solid #eee",
        padding: "12px 24px",
        display: "flex",
        alignItems: "center",
        justifyContent: "space-between"
      }}
    >
      {/* Logo */}
      <Link
        to="/"
        style={{
          fontSize: "1.4rem",
          fontWeight: 700,
          color: "#d94800",
          textDecoration: "none"
        }}
      >
        The Food Advisor
      </Link>

      {/* Desktop Menu */}
      <div className="desktop-menu" style={{ display: "flex", gap: "24px" }}>
        <Link className="nav-link" to="/trending">Trending</Link>
        <Link className="nav-link" to="/highlights">City Highlights</Link>
        <Link className="nav-link" to="/awards">Awards</Link>
        <Link className="nav-link" to="/city-guide">City Food Guide</Link>
        <Link className="nav-link" to="/owner">For Restaurants</Link>
      </div>

      {/* Mobile Hamburger */}
      <div
        className="hamburger"
        onClick={() => setOpen(!open)}
        style={{
          display: "none",
          cursor: "pointer",
          fontSize: "1.8rem",
          userSelect: "none"
        }}
      >
        ☰
      </div>

      {/* Mobile Menu */}
      {open && (
        <div
          className="mobile-menu"
          style={{
            position: "absolute",
            top: "64px",
            right: 0,
            background: "#fff",
            width: "200px",
            boxShadow: "0 4px 12px rgba(0,0,0,0.1)",
            borderRadius: "12px",
            padding: "16px",
            display: "flex",
            flexDirection: "column",
            gap: "12px"
          }}
        >
          <Link className="nav-link" to="/trending">Trending</Link>
          <Link className="nav-link" to="/highlights">City Highlights</Link>
          <Link className="nav-link" to="/awards">Awards</Link>
          <Link className="nav-link" to="/city-guide">City Food Guide</Link>
          <Link className="nav-link" to="/owner">For Restaurants</Link>
        </div>
      )}

      {/* Styles */}
      <style>
        {`
          .nav-link {
            text-decoration: none;
            color: #333;
            font-weight: 500;
            transition: color 0.2s ease;
          }

          .nav-link:hover {
            color: #d94800;
          }

          @media (max-width: 768px) {
            .desktop-menu {
              display: none;
            }
            .hamburger {
              display: block;
            }
          }
        `}
      </style>
    </nav>
  );
}
