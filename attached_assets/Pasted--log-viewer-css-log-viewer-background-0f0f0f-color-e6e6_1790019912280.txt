/* log-viewer.css */
.log-viewer {
  background: #0f0f0f;
  color: #e6e6e6;
  font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
}

/* groups */
.log-group-header {
  padding: 8px 16px;
  border-bottom: 1px solid #1c1c1c;
  cursor: pointer;
}

.log-group-body {
  transition: max-height 150ms ease, opacity 150ms ease;
}

.log-group-body-open {
  max-height: 1000px;
  opacity: 1;
}

.log-group-body-closed {
  max-height: 0;
  opacity: 0;
  overflow: hidden;
}

/* rows */
.log-row {
  padding: 12px 16px;
  border-bottom: 1px solid #1c1c1c;
  display: flex;
  flex-direction: column;
  background: #0f0f0f;
  transition: background 120ms ease;
}

.log-row-hover {
  background: #151515;
}

.log-row-expanded {
  background: #0d0d0d;
}

.log-row-main {
  display: flex;
  align-items: center;
  gap: 8px;
}

.log-row-content {
  flex: 1;
}

.log-row-summary {
  font-size: 0.95rem;
}

.log-row-meta {
  font-size: 0.8rem;
  color: #9a9a9a;
  display: flex;
  gap: 8px;
}

.log-row-actions {
  display: flex;
  gap: 4px;
}

/* severity */
.log-severity-dot {
  width: 6px;
  height: 6px;
  border-radius: 999px;
}

.log-severity-info {
  background: #3a82f7;
}

.log-severity-warn {
  background: #f7c948;
}

.log-severity-error {
  background: #e5484d;
}

/* icons */
.log-copy-icon,
.log-context-menu button {
  background: transparent;
  border: none;
  color: #9a9a9a;
  font-size: 0.8rem;
  cursor: pointer;
}

/* inline expansion */
.log-inline-expansion {
  padding: 12px 16px; /* match row */
}

.log-inline-code {
  margin: 0;
  background: #111111;
  color: #dcdcdc;
  font-family: "JetBrains Mono", "Fira Code", monospace;
  font-size: 0.8rem;
  overflow-x: auto;
}

/* simple syntax colours via tokenization or highlighter */
.log-inline-show-more {
  margin-top: 4px;
  background: transparent;
  border: none;
  color: #7abaff;
  font-size: 0.8rem;
  cursor: pointer;
}
